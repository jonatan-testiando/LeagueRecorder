# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.key
*.key.pub

# Bundled binaries
src-tauri/bin/

# Python runtime embebido (se construye con python_scripts/setup_runtime.ps1)
src-tauri/python-runtime/

# Runtime de OBS empaquetado (se ensambla con leaguerec-obs/scripts/assemble-runtime.ps1)
src-tauri/obs-runtime/
python_scripts/__pycache__/

# Template de referencia de diseño (no forma parte de la app)
/compute-the-platform-to-build-and-ship-ai-agents/

# OBS compilado desde fuente para leaguerec-obs (enorme; se reconstruye con build-obs.ps1)
/third_party/
leaguerec-obs/build/

# Datasets/entrenamiento YOLO (artefactos grandes, no van a git)
/dataset/
/runs/
# los .onnx de modelos: ignoramos todos menos el que usa la app (versionado)
/models/*.onnx
!/models/cursor_multi_fp32.onnx
*.pt
.venv-train/
